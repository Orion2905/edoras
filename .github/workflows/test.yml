name: Test GitHub Actions

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Test GitHub Actions
      run: |
        echo "🚀 GitHub Actions funziona!"
        echo "Repository: ${{ github.repository }}"
        echo "Branch: ${{ github.ref }}"
        ls -la
